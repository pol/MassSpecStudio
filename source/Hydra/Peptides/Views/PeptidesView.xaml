<AvalonDock:DockableContent x:Class="Hydra.Modules.Peptides.Views.PeptidesView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:AvalonDock="clr-namespace:AvalonDock;assembly=AvalonDock"
			 mc:Ignorable="d" 
							Title="Peptides Window"
			 d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<ListView ItemsSource="{Binding Peptides}" SelectedItem="{Binding SelectedPeptide}">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Sequence" DisplayMemberBinding="{Binding Sequence}"></GridViewColumn>
					<GridViewColumn Header="Start" DisplayMemberBinding="{Binding AminoAcidStart}"></GridViewColumn>
					<GridViewColumn Header="Stop" DisplayMemberBinding="{Binding AminoAcidStop}"></GridViewColumn>
					<GridViewColumn Header="M/Z" DisplayMemberBinding="{Binding MonoIsotopicMass}"></GridViewColumn>
					<GridViewColumn Header="Charge" DisplayMemberBinding="{Binding ChargeState}"></GridViewColumn>
					<GridViewColumn Header="RT" DisplayMemberBinding="{Binding RT}"></GridViewColumn>
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</AvalonDock:DockableContent>
